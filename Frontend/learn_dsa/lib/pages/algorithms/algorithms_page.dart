import 'package:flutter/material.dart';

class AlgorithmsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Algorithms Page', style: TextStyle(fontSize: 24)),
    );
  }
}
