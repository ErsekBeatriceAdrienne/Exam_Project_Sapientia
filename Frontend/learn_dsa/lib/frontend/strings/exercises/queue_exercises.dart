import 'package:flutter/material.dart';

class QueueExercisesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Queue Exercises")),
      body: Center(
        child: Text(
          "",
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}