import 'package:flutter/material.dart';

class BSTExercisesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Binary Trees Exercises")),
      body: Center(
        child: Text(
          "",
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}