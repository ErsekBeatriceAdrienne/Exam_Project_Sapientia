import 'package:flutter/material.dart';

class QueueTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Queue Test")),
      body: Center(
        child: Text(
          "",
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}