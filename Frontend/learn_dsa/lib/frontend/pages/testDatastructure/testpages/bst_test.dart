import 'package:flutter/material.dart';

class BstTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Binary Search Tree Test")),
      body: Center(
        child: Text(
          "",
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}