import 'package:flutter/material.dart';

class ArrayExercisesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Array Exercises")),
      body: Center(
        child: Text(
          "",
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}